'use client';
import { Download, Maximize2, RefreshCw, Save } from 'lucide-react';
export default function DesignCard({image,index,onRegenerate}:{image:string;index:number;onRegenerate:()=>void}){const download=()=>{const a=document.createElement('a');a.href=image;a.download=`textile-design-${index+1}.png`;a.click()};return <article className="design-card"><img className="design-image" src={image} alt={`AI textile design ${index+1}`}/><div className="design-body"><div className="design-meta"><span>DESIGN {String(index+1).padStart(2,'0')}</span><span>AI VARIATION</span></div><div className="card-actions"><button onClick={onRegenerate}><RefreshCw size={12}/> Regenerate This</button><button onClick={download}><Download size={12}/> Download</button><button onClick={()=>window.open(image,'_blank')}><Maximize2 size={12}/> Full Screen</button><button onClick={()=>localStorage.setItem(`textile-design-${index+1}`,image)}><Save size={12}/> Save</button></div></div></article>}
