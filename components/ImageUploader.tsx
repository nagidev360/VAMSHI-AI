'use client';
import { UploadCloud, X } from 'lucide-react';
import { useRef } from 'react';
export default function ImageUploader({file,onChange}:{file:File|null;onChange:(f:File|null)=>void}){const ref=useRef<HTMLInputElement>(null);return <div>{file?<div><img className="preview" src={URL.createObjectURL(file)} alt="Reference preview"/><div className="card-actions"><button onClick={()=>ref.current?.click()}>Replace Image</button><button onClick={()=>onChange(null)}><X size={13}/> Remove Image</button></div></div>:<div className="dropzone" onClick={()=>ref.current?.click()} onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();const f=e.dataTransfer.files[0];if(f)onChange(f)}}><input ref={ref} type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>onChange(e.target.files?.[0]||null)}/><div className="drop-icon"><UploadCloud/></div><strong>Drop your textile reference here</strong><p className="muted">JPG, JPEG, PNG or WEBP · no prompt required</p></div>}</div>}
