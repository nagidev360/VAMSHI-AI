import sharp from 'sharp';
export async function inspectImage(buffer:Buffer){const meta=await sharp(buffer).metadata();return {format:meta.format,width:meta.width,height:meta.height,hasAlpha:meta.hasAlpha,aspectRatio:meta.width&&meta.height?Number((meta.width/meta.height).toFixed(3)):null};}
export function buildFallbackAnalysis(meta:Awaited<ReturnType<typeof inspectImage>>){return {style:'professional textile print',motifs:['preserve the reference motif family without copying'],colors:['derive a harmonious palette from the reference'],pattern:'balanced repeating surface pattern',layout:'clean commercial textile composition',decorativeElements:['retain the reference visual language'],density:'medium',symmetry:'match reference balance',fabricCharacteristics:'print-ready textile surface',imageMeta:meta};}
