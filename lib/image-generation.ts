export interface ImageGenerationProvider{generate(instruction:string,reference?:string,highResolution?:boolean):Promise<string>}
export class OpenAIImageProvider implements ImageGenerationProvider{async generate(instruction:string,reference?:string,highResolution=false){const key=process.env.IMAGE_GENERATION_API_KEY;if(!key)throw new Error('Image generation is not configured. Add IMAGE_GENERATION_API_KEY on the server.');const endpoint=process.env.IMAGE_GENERATION_BASE_URL||'https://api.openai.com/v1/images/generations';const model=process.env.IMAGE_GENERATION_MODEL||'gpt-image-1';const prompt=`Professional commercial textile surface design. ${instruction}. Use the supplied reference only as inspiration; create a genuinely new design, not a copy. Flat fabric-print artwork, clean edges, repeat-aware composition, no mockup, no garment, no text.`;const body:any={model,prompt,n:1,size:highResolution?'1536x1024':'1024x1024'};if(reference)body.input_fidelity='high';const res=await fetch(endpoint,{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify(body)});if(!res.ok)throw new Error('Image generation provider returned an error');const data=await res.json();const item=data.data?.[0];if(item?.b64_json)return `data:image/png;base64,${item.b64_json}`;if(item?.url)return item.url;throw new Error('Image generation returned no image');}}
export function getImageProvider():ImageGenerationProvider{return new OpenAIImageProvider();}
