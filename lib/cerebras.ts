type Analysis={style:string;motifs:string[];colors:string[];pattern:string;layout:string;decorativeElements:string[];density:string;symmetry:string;fabricCharacteristics:string;imageMeta?:unknown};
export async function analyzeWithCerebras(context:unknown,options:{mode:string;color:string;repeat:string}){const key=process.env.CEREBRAS_API_KEY;if(!key)return null;const prompt=`You are a textile design director. Analyze this machine-extracted reference metadata and create six distinct, commercially useful textile variation specifications. Do not copy the reference. Preserve its design language while changing composition, motif arrangement and repeat. Mode: ${options.mode}. Color: ${options.color}. Repeat: ${options.repeat}. Return ONLY JSON with keys style,motifs,colors,pattern,layout,decorativeElements,density,symmetry,fabricCharacteristics,variations. variations must contain exactly 6 objects with title,composition,motifTreatment,colorTreatment,repeatTreatment,generationInstruction.`;const res=await fetch('https://api.cerebras.ai/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({model:process.env.CEREBRAS_MODEL||'llama-4-scout-17b-16e-instruct',temperature:.8,messages:[{role:'system',content:prompt},{role:'user',content:JSON.stringify(context)}],response_format:{type:'json_object'}})});if(!res.ok)throw new Error('Cerebras analysis failed');const data=await res.json();return JSON.parse(data.choices?.[0]?.message?.content||'{}') as Analysis&{variations:Array<Record<string,string>>};}
